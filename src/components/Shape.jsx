import React from "react";
import Snap from "snapsvg";

export default function Shape(props){
    var s = Snap("#shape");

    return(
        <svg className="shape" id="shape">
            
        </svg>
    )
}